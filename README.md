# mnist-pytorch-lab

Minimal usage to run tests.

```bash
pip install -r requirements.txt
python -m pytest
```

Note: There are currently no automated tests in this repo, so `pytest` will report zero tests.
